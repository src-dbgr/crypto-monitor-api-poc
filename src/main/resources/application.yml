app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/coin
    username: postgres
    password: password
    pool-size: 30

springdoc:
  swagger-ui:
    path: /api-docs

spring:
  profiles:
    active: dev